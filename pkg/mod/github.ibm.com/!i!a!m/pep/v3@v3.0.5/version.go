package pep

// version is sent to the pdp for analytical purpose.
// Generally this should be the same version as the git tag value when publishing the pep.
// Thus before each release, this must be changed accordingly
var version string = "v3.0.5"
